struct A {
    int x;
    int y;
    bool b;
    struct A s;
};

struct B {
    int qr;
    int y;
    bool b;
    struct A s;
};

struct B data;
struct A abc;
bool f;

fun main() int {
    struct A data;
    data.x = 5;
    data.s.s.s = 5;
}

fun foo(int x, bool y, struct A obj) int {
    struct A data;
    data.x = 5;
}
